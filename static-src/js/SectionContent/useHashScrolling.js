export default function useHashScrolling() {
  //  - detects hash changes and scrolls to content when the hash updates to something
  //  - handles initial scroll to content from hash or redux
  //  - detects when the user has scrolled and updates the hash (and redux?) accordingly

  return () => null;
}
