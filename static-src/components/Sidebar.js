import React from 'react';
import styled from 'styled-components';

export default function Sidebar() {
  return <StyledSidebar>StyledSidebar</StyledSidebar>;
}

const StyledSidebar = styled.div`
  background-color: #eff;
`;
